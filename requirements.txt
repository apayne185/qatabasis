# NOTE: not currently installed by any tested path (Dockerfile has its own
# pinned pip install list; scripts/install_native.sh uses environment.yml).
# Kept as a reference/reproducibility record and for anyone doing a manual
# `pip install -r requirements.txt` outside Docker/conda. If you change
# Dockerfile's pins, update this file to match (or vice versa) so they
# don't drift out of sync again -- see Dockerfile's own pin comment for
# the 2026-09-22 verification this and it were both checked against.

# Core quantum
qiskit>=2.5.2,<3.0
qiskit-nature>=0.8.0
qiskit-ibm-runtime>=0.49.0
pyscf>=2.14.0

# HPC / parallel
mpi4py>=4.1.2
numpy>=2.5.3
scipy>=1.18.1

# Testing
pytest>=9.1.1

# Optional: GPU acceleration (requires CUDA 12.2)
# cupy-cuda12x>=12.0

# Optional: molecule resolution via SMILES
# rdkit

# Optional: results analysis plots
# matplotlib
pennylane
pennylane-lightning
pennylane-lightning-gpu>=3.7
